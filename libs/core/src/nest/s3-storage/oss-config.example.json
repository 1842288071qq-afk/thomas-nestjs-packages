{
  "aliyun": {
    "name": "阿里云 OSS 生产",
    "code": "aliyun_prod",
    "bucket": "replace-with-bucket",
    "endpoint": "https://oss-cn-hangzhou.aliyuncs.com",
    "remark": "AK/SK 请通过安全配置流程写入，不要提交真实凭证",
    "config": {
      "accessKeyId": "replace-with-access-key-id",
      "secretAccessKey": "replace-with-secret-access-key",
      "region": "cn-hangzhou",
      "provider": "aliyun",
      "addressingStyle": "virtual-hosted",
      "domain": "https://cdn.example.com",
      "signingExpiresIn": 900,
      "multipartChunkSize": 8388608
    }
  },
  "s3PathStyle": {
    "name": "S3 兼容存储",
    "code": "s3_compatible",
    "bucket": "replace-with-bucket",
    "endpoint": "https://s3.example.com",
    "config": {
      "accessKeyId": "replace-with-access-key-id",
      "secretAccessKey": "replace-with-secret-access-key",
      "region": "us-east-1",
      "provider": "s3",
      "addressingStyle": "path",
      "signingExpiresIn": 900,
      "multipartChunkSize": 8388608
    }
  }
}
